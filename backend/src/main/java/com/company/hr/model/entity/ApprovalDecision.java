package com.company.hr.model.entity;

/** Unified outcome for both stages — a peer's ACCEPT/DECLINE and a manager's APPROVE/REJECT both map to this. */
public enum ApprovalDecision {
    APPROVED,
    REJECTED
}
